Fetch documentation for a library:

Given library name "$ARGUMENTS" (e.g., "pixijs", "mongoose", "express"):

1. Use WebSearch to find the official documentation
2. Fetch key API reference pages
3. Summarize the most relevant parts for CryptoTribes project
4. If it's about PixiJS - focus on sprites, containers, and isometric rendering
5. If it's about Mongoose - focus on schemas, queries, and transactions
6. If it's about Express - focus on routing and middleware

Provide code examples relevant to our tech stack.
