Start the CryptoTribes development server:

1. Check if MongoDB is accessible
2. Run `node server/server.js` in background
3. Wait for "CryptoTribes запущен!" message
4. Report the server URL (http://localhost:3000)
5. Open browser with Puppeteer and take a screenshot of the login page

If there are errors, fix them and retry.
