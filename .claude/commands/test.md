Run all tests for the CryptoTribes project:

1. First check if server is running and stop it if needed
2. Run `npm test`
3. Report the results including:
   - Number of tests passed/failed
   - Coverage summary
   - Any specific failures with details

If tests fail, analyze the errors and suggest fixes.
